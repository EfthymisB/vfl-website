{
  "name": "vfl-website",
  "version": "1.0.0",
  "description": "Website for the VFX Football League (VFL)",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "eleventy --serve --quiet",
    "dev": "eleventy --serve --quiet",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "luxon": "^3.5.0"
  }
}
